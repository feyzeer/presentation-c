
#include <stdio.h>

int main()

{
    int nbr;
    printf("donner un entier");
    scanf("%d",&nbr);
    if(nbr<0)
    printf("le nombre est negatif");
    else if(nbr>0)
    printf("le nombre es positif");
    else 
    printf("le nombre est nul");
    

    return 0;
}
