#include <unistd.h>
#include <stdio.h>

int main(){
    
    float moyenne;

    printf("La note : \n");
    scanf("%f", & moyenne);

    if ( moyenne >= 16)
        printf("Tres bien\n");
    else if ( moyenne >= 10 &&  moyenne < 12)
        printf("passable\n");
    else if ( moyenne >= 12 &&  moyenne < 14)
        printf("Assez bien\n");
    else if ( moyenne >= 14 &&  moyenne < 16)
        printf("Bien\n");
    else if ( moyenne < 0)
        printf("nul\n");

    return 0;
}